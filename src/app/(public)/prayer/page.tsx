'use client';

const PrayerPage: React.FC = () => {
	return (
		<section className='content-section mt-content mb-content'>
			<header>
				<h1 className='headline-primary'>Prayer</h1>
			</header>
		</section>
	);
};

export default PrayerPage;
