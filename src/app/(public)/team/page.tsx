'use client';

const TeamPage: React.FC = () => {
	return (
		<section className='content-section mt-content mb-content'>
			<header>
				<h1 className='headline-primary'>Team</h1>
			</header>
		</section>
	);
};

export default TeamPage;
